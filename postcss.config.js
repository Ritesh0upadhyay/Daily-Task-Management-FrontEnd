export default {
  //Export the configuration
  plugins: {
    //Tell PostCSS which plugins to use
    tailwindcss: {}, //Process Tailwind CSS
    autoprefixer: {}, //Automatically add browser prefixes to CSS (e.g., -webkit-, -moz-)
  },
};
